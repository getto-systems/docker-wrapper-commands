#!/bin/bash
. docker-wrapper.sh

docker_wrapper_ide(){
  local cmd=$1; shift

  if [ -z "$cmd" ]; then
    cmd=restart
  fi

  if [ "$(LC_ALL=C type -t DOCKER_WRAPPER_IDE)" == function ]; then
    DOCKER_WRAPPER_IDE "$cmd" "$@"
  fi
}

docker_wrapper_ide "$@"
